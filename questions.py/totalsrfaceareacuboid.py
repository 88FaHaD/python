l = float(input('enter the length of left/right side '))
r = float(input('enter the height of left/right side '))
f = float(input('enter the length of front/back side '))
b = float(input('enter the height of  back/front side '))
t = float(input('enter the length of top/bottom side '))
bo = float(input('enter the length of bottom/top side '))

totalsurfacearea=2 *((l*r) +(f*b) + (t*bo))

print('total area is ', totalsurfacearea)